function Me() {
  const mystyle = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  };

  return (
    <div>
      <h1 style={mystyle}>My Profile</h1>
    </div>
  );
  }

export default Me;
